# 智能运维 Agent 平台 · 项目说明书

## 1. 项目概述

### 1.1 项目背景
本项目是一个基于大语言模型（LLM）的智能运维 Agent 平台 Demo，旨在通过自然语言交互完成运维场景下的常见任务，包括天气查询、运维数据查询（NL2SQL）、故障诊断等。系统具备用户鉴权、多轮对话记忆、多工具编排、反幻觉校验等能力，前端采用 Vue 3 单页应用，后端采用 FastAPI。

### 1.2 项目目标
- 用户注册 / 登录 / JWT 鉴权，会话数据按用户隔离
- 新建 / 切换 / 重命名 / 删除会话，历史消息持久化到 MySQL
- 多轮对话上下文注入，支持工具调用的可观测与审计
- 意图识别 + 多工具编排（天气、NL2SQL、故障诊断）
- 反幻觉输出校验（数值来源回溯 + 工具失败如实告知）
- 前端工程化（Vue 3 + Vite + TypeScript + Pinia + Element Plus）

### 1.3 项目范围
| 类型 | 内容 |
|---|---|
| In Scope | 用户注册/登录、JWT、会话 CRUD、消息落库、意图识别、工具注册、NL2SQL、故障诊断、Vue 3 前端 |
| Out of Scope | OAuth/SSO、RBAC 多角色、模型微调、生产级高可用、监控告警平台对接 |

---

## 2. 技术栈

| 层级 | 技术 | 用途 |
|---|---|---|
| Web 框架 | FastAPI | 异步路由 + 依赖注入鉴权 |
| ORM | SQLAlchemy 2.x + PyMySQL | MySQL 操作 |
| 鉴权 | python-jose (JWT) + bcrypt | token 签发校验 + 密码哈希 |
| 校验 | Pydantic v2 | 请求体、工具结果 schema |
| LLM 编排 | LangChain | ChatPromptTemplate、@tool、ToolMessage |
| LLM 接入 | langchain_openai → 硅基流动 | OpenAI 兼容接口 |
| 外部工具 | 高德天气 API + 内置 SQLite | 实时天气 + 运维数据查询 |
| SQL 安全 | sqlparse | NL2SQL 语法解析 + 黑名单拦截 |
| 配置 | python-dotenv | 加载 .env |
| 前端框架 | Vue 3 + Vite | 单页应用构建 |
| 前端语言 | TypeScript | 类型安全 |
| 前端路由 | Vue Router 4 | 页面路由 + 登录守卫 |
| 状态管理 | Pinia | 用户/会话/消息状态 |
| UI 组件库 | Element Plus | 表单、对话框、消息提示等 |
| HTTP 客户端 | Axios | 请求封装 + 拦截器 |
| 数据库 | MySQL 5.7 | 用户/会话/消息/日志持久化 |

后端依赖：
```bash
pip install fastapi uvicorn sqlalchemy pymysql python-jose[cryptography] bcrypt python-dotenv langchain langchain-openai requests sqlparse
```

前端依赖见 `frontend/package.json`。

---

## 3. 系统架构

```
浏览器 (Vue 3 SPA: frontend/)
  ├─ Vue Router：/login /register /chat
  ├─ Pinia：userStore / sessionStore / chatStore
  ├─ Axios 拦截器：注入 JWT / 401 跳登录
  └─ Element Plus + 自定义聊天组件
        │  Authorization: Bearer <jwt>
        ▼
FastAPI (controller.py)
  ├─ get_current_user（JWT 校验）
  ├─ POST /api/auth/register|login
  ├─ POST /api/sessions        新建
  ├─ GET  /api/sessions        列表
  ├─ GET  /api/sessions/{id}/messages  历史
  ├─ PATCH /api/sessions/{id}  重命名
  ├─ DELETE /api/sessions/{id} 软删除
  └─ POST /api/chat  {session_id, message}
        ▼
Agent Orchestrator (agent.py)
  ├─ load_chat_history (MySQL 读最近 20 条)
  ├─ IntentClassifier (intent.py)
  ├─ Prompt (system + history + input)
  ├─ LLM bind_tools(registry.all_tools())
  │     ├─ get_weather      (高德 API)
  │     ├─ nl2sql_query     (SQLite + sqlparse)
  │     ├─ generate_sql     (仅生成 SQL，不执行)
  │     └─ diagnose_fault   (fault_kb.json 知识库)
  ├─ 多工具循环（上限 3 次）+ 写 tool_call_logs
  ├─ OutputValidator (output_validator.py 反幻觉)
  └─ 持久化 messages 表
        ▼
   MySQL 5.7
   users / sessions / messages / tool_call_logs
```

---

## 4. 目录结构

```
MachineLearning/
├── controller.py          # FastAPI 入口：鉴权 + 会话 CRUD + 聊天
├── agent.py               # Agent 编排核心：意图识别、工具循环、持久化
├── auth.py                # 密码哈希、JWT 签发/校验、当前用户依赖
├── database.py            # SQLAlchemy 引擎、SessionLocal、get_db
├── models.py              # ORM 模型：User / Session / Message / ToolCallLog
├── schemas.py             # Pydantic 请求/响应模型
├── intent.py              # 意图识别器（LLM 分类）
├── tool_registry.py       # 工具注册中心（单例）
├── output_validator.py    # 反幻觉输出校验（数值来源回溯）
├── nl2sql.py              # NL2SQL 工具（生成 + 校验 + 执行 SQLite）
├── diagnose.py            # 故障诊断工具（知识库关键词匹配）
├── init_ops_db.py         # 初始化 NL2SQL 示例 SQLite 库
├── fault_kb.json          # 故障诊断知识库
├── ops.db                 # NL2SQL 示例 SQLite 数据库（git 忽略，可重新生成）
├── sql/
│   └── init.sql           # MySQL 初始化脚本（四张表）
├── docs/
│   └── PRD-Agent-Ops.md   # 需求文档
├── frontend/              # Vue 3 前端工程
│   ├── src/
│   │   ├── api/           # 接口层（auth / chat / session / request）
│   │   ├── components/    # ChatBubble / ChatInput / SessionList / ToolLogPanel
│   │   ├── layouts/       # ChatLayout
│   │   ├── router/        # Vue Router + 登录守卫
│   │   ├── stores/        # Pinia：user / session / chat
│   │   ├── types/         # TypeScript 类型定义
│   │   └── views/         # LoginView / RegisterView / ChatView
│   ├── vite.config.ts     # Vite 配置（含 /api 代理到 8000）
│   └── package.json
├── .gitignore
├── .env                   # 环境变量（不入仓，见下方格式说明）
├── 项目说明书.md
└── 操作说明书.md
```

---

## 5. 环境变量配置（.env）

项目通过根目录的 `.env` 文件加载所有配置，`python-dotenv` 在 `database.py` 启动时校验必需变量，缺失则 fail-fast。`.env` 已被 `.gitignore` 忽略，不会提交到仓库。

### 5.1 .env 文件格式

```ini
# ============ LLM（硅基流动，OpenAI 兼容接口）============
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL_NAME=deepseek-ai/DeepSeek-V4-Pro

# ============ 高德天气 API ============
AMAP_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxx
AMAP_WEATHER_URL=https://restapi.amap.com/v3/weather/weatherInfo

# ============ MySQL 数据库 ============
MYSQL_DSN=mysql+pymysql://root:your_password@127.0.0.1:3306/agent_ops?charset=utf8mb4

# ============ JWT 鉴权 ============
JWT_SECRET=please-change-this-to-a-random-secret-string
JWT_EXP_DAYS=7

# ============ 可选配置 ============
# NL2SQL 示例 SQLite 库路径（默认项目根目录下的 ops.db）
# NL2SQL_DB_PATH=./ops.db
# 故障诊断知识库路径（默认项目根目录下的 fault_kb.json）
# FAULT_KB_PATH=./fault_kb.json
```

### 5.2 变量说明

| 变量名 | 必填 | 说明 |
|---|---|---|
| `SILICONFLOW_API_KEY` | 是 | 硅基流动平台 API Key |
| `SILICONFLOW_BASE_URL` | 是 | 硅基流动 API 地址，固定 `https://api.siliconflow.cn/v1` |
| `SILICONFLOW_MODEL_NAME` | 是 | 模型名称，如 `deepseek-ai/DeepSeek-V4-Pro` |
| `AMAP_API_KEY` | 是 | 高德开放平台 Web 服务 Key |
| `AMAP_WEATHER_URL` | 是 | 高德天气查询接口地址 |
| `MYSQL_DSN` | 是 | MySQL 连接串，格式 `mysql+pymysql://用户:密码@主机:端口/库名?charset=utf8mb4` |
| `JWT_SECRET` | 是 | JWT 签名密钥，**生产环境务必修改为随机字符串** |
| `JWT_EXP_DAYS` | 否 | JWT 过期天数，默认 7 |
| `NL2SQL_DB_PATH` | 否 | NL2SQL 示例 SQLite 库路径，默认 `./ops.db` |
| `FAULT_KB_PATH` | 否 | 故障诊断知识库 JSON 路径，默认 `./fault_kb.json` |

---

## 6. 核心模块说明

### 6.1 鉴权模块（auth.py）
- `hash_password` / `verify_password`：bcrypt 加盐哈希
- `create_access_token`：HS256 签发 JWT，`sub=user_id`，默认 7 天过期
- `get_current_user`：FastAPI 依赖，从 `Authorization: Bearer <token>` 解析并校验用户，失败返回 401

### 6.2 Agent 编排（agent.py）
- `load_chat_history`：从 MySQL 拉取该 session 最近 20 条 human/ai 消息作为上下文
- `test_chat_openai` 主流程：
  1. 意图识别（IntentClassifier）
  2. 拉取历史 → 组装 Prompt
  3. 持久化 human 消息
  4. 多工具循环（上限 3 次）：LLM 决策 → 执行工具 → 写 tool_call_logs → 再调 LLM
  5. 反幻觉校验（OutputValidator），不通过则追加约束提示重试 1 次
  6. 兜底空回复，持久化 ai 回答

### 6.3 意图识别（intent.py）
- LLM 对用户输入做分类，输出 `{chat, weather, nl2sql, diagnose, unknown}` + 置信度
- 置信度 < 0.6 回退为 `chat`

### 6.4 工具注册中心（tool_registry.py）
- 单例 `ToolRegistry`，`register(tool)` 注册 LangChain `@tool`
- `all_tools()` 一次性绑定到 LLM，`get(name)` 按名称执行

### 6.5 NL2SQL（nl2sql.py）
- `nl2sql_query`：自然语言 → LLM 生成 SQL → 安全校验 → 执行 SQLite → 自然语言回显
- `generate_sql`：仅生成 SQL 语句，不执行
- 安全校验：黑名单（DROP/DELETE/TRUNCATE/ALTER/GRANT/UPDATE/REPLACE/RENAME/INSERT/CREATE）+ 仅 SELECT + 强制 LIMIT ≤ 100

### 6.6 故障诊断（diagnose.py）
- 基于 `fault_kb.json` 关键词匹配，返回分步骤排查清单 + 多轮追问建议
- 支持 6 类常见故障：CPU 高、内存不足、磁盘满、服务宕机、网络不通、数据库慢

### 6.7 反幻觉校验（output_validator.py）
- 提取回答中的数值，检查是否能在工具结果中找到来源（含浮点容错）
- 工具失败时回答不应包含未来源数值
- 校验不通过由 agent.py 重试 1 次

---

## 7. 数据库设计（MySQL 5.7）

详见 `sql/init.sql`，核心四张表：

### 7.1 users 用户表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 自增主键 |
| username | VARCHAR(64) UNIQUE | 用户名 |
| password | VARCHAR(128) | bcrypt 哈希 |
| created_at | DATETIME | 创建时间 |

### 7.2 sessions 会话表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | CHAR(36) PK | UUID |
| user_id | BIGINT FK | 归属用户 |
| title | VARCHAR(128) | 会话标题（默认"新对话"，首条消息取前 12 字） |
| is_deleted | TINYINT(1) | 软删除标记 |
| created_at / updated_at | DATETIME | 时间戳 |
| 索引 | idx_user_updated | (user_id, updated_at) |

### 7.3 messages 消息表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 自增 |
| session_id | CHAR(36) FK | 所属会话 |
| role | ENUM('human','ai','tool','system') | 消息角色 |
| content | TEXT | 消息内容 |
| tool_calls | JSON | ai 消息的工具调用决策 |
| created_at | DATETIME | 创建时间 |

### 7.4 tool_call_logs 工具调用日志
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 自增 |
| session_id | CHAR(36) FK | 所属会话 |
| tool_name | VARCHAR(64) | 工具名 |
| args | JSON | 调用参数 |
| result | TEXT | 工具返回结果 |
| latency_ms | INT | 耗时（毫秒） |
| created_at | DATETIME | 调用时间 |

---

## 8. API 接口设计

所有 `/api/*` 接口（除注册登录外）需在 Header 携带 `Authorization: Bearer <token>`。

### 8.1 鉴权
| 方法 | 路径 | 请求体 | 响应 |
|---|---|---|---|
| POST | /api/auth/register | `{username, password}` | `{user_id, username}` |
| POST | /api/auth/login | `{username, password}` | `{token, user:{user_id, username}}` |
| GET | /api/auth/me | - | `{user_id, username}` |

### 8.2 会话
| 方法 | 路径 | 请求体 | 响应 |
|---|---|---|---|
| POST | /api/sessions | - | `{session_id, title}` |
| GET | /api/sessions | - | `[{id, title, updated_at}]` |
| GET | /api/sessions/{id}/messages | `?page&size` | `[{role, content, ts}]` |
| PATCH | /api/sessions/{id} | `{title}` | `{ok}` |
| DELETE | /api/sessions/{id} | - | `{ok}`（软删除） |

### 8.3 对话
| 方法 | 路径 | 请求体 | 响应 |
|---|---|---|---|
| POST | /api/chat | `{session_id, message}` | `{reply, intent, tools:[{name, args, result, ms}]}` |

---

## 9. 安全约束

1. **密码**：bcrypt 加盐哈希，不存明文
2. **JWT**：HS256，exp 默认 7 天，`JWT_SECRET` 走 `.env`
3. **越权防护**：`_get_user_session` 校验 `session.user_id == user.id`，越权返回 403，不存在返回 404
4. **SQL 注入**：ORM 参数化 + NL2SQL 黑名单 + 仅 SELECT + LIMIT ≤ 100
5. **密钥**：`.env` 不入仓，启动时校验必需变量缺失 fail-fast
6. **日志脱敏**：tool_call_logs 不记录密钥

---

## 10. 前端工程结构（Vue 3）

| 目录 | 职责 |
|---|---|
| `api/request.ts` | Axios 实例 + 请求/响应拦截器（注入 token、401 跳登录） |
| `api/auth.ts` | 注册 / 登录 / 获取当前用户 |
| `api/session.ts` | 会话 CRUD + 历史消息 |
| `api/chat.ts` | 发送消息 |
| `stores/user.ts` | 用户信息 + token（localStorage 持久化） |
| `stores/session.ts` | 会话列表 + 当前会话 |
| `stores/chat.ts` | 消息列表 + 发送状态 |
| `components/SessionList.vue` | 会话列表（含 hover 删除按钮） |
| `components/ChatBubble.vue` | 消息气泡 + 工具日志折叠面板 |
| `components/ChatInput.vue` | 输入框（Enter 发送 / Shift+Enter 换行） |
| `layouts/ChatLayout.vue` | 聊天页整体布局（侧边栏 + 主区） |
| `router/index.ts` | 路由表 + 全局登录守卫 |

### 关键设计约束
- **Axios 统一封装**：请求拦截器从 localStorage 取 token 注入 header；响应拦截器对 401 清 token 跳 `/login`
- **路由守卫**：未登录且目标非白名单（`/login`、`/register`）则重定向到 `/login`
- **Vite 代理**：开发环境 `/api` 代理到 `http://127.0.0.1:8000`，解决跨域
- **Token 持久化**：存 localStorage，刷新页面不丢失登录态
