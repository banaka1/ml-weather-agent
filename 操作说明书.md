# 智能运维 Agent 平台 · 操作说明书

## 1. 环境准备

### 1.1 软件要求
| 软件 | 版本要求 |
|---|---|
| Python | 3.9+ |
| Node.js | 16+ |
| MySQL | 5.7+（推荐 5.7.26） |
| 浏览器 | Chrome / Edge 最新版 |

### 1.2 第三方服务
- **硅基流动 API Key**：用于 LLM 调用（OpenAI 兼容接口），申请地址 https://cloud.siliconflow.cn/
- **高德天气 API Key**：用于实时天气查询，申请地址 https://console.amap.com/

---

## 2. 环境配置

### 2.1 创建 .env 文件
在项目根目录创建 `.env` 文件（已被 `.gitignore` 忽略，不会入仓）。完整格式如下：

```ini
# ============ LLM（硅基流动，OpenAI 兼容接口）============
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL_NAME=deepseek-ai/DeepSeek-V4-Pro

# ============ 高德天气 API ============
AMAP_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxx
AMAP_WEATHER_URL=https://restapi.amap.com/v3/weather/weatherInfo

# ============ MySQL 数据库 ============
MYSQL_DSN=mysql+pymysql://root:your_password@127.0.0.1:3306/agent_ops?charset=utf8mb4

# ============ JWT 鉴权 ============
JWT_SECRET=please-change-this-to-a-random-secret-string
JWT_EXP_DAYS=7

# ============ 可选配置 ============
# NL2SQL 示例 SQLite 库路径（默认项目根目录下的 ops.db）
# NL2SQL_DB_PATH=./ops.db
# 故障诊断知识库路径（默认项目根目录下的 fault_kb.json）
# FAULT_KB_PATH=./fault_kb.json
```

**变量说明**：

| 变量名 | 必填 | 说明 |
|---|---|---|
| `SILICONFLOW_API_KEY` | 是 | 硅基流动平台 API Key |
| `SILICONFLOW_BASE_URL` | 是 | 硅基流动 API 地址，固定 `https://api.siliconflow.cn/v1` |
| `SILICONFLOW_MODEL_NAME` | 是 | 模型名称，如 `deepseek-ai/DeepSeek-V4-Pro` |
| `AMAP_API_KEY` | 是 | 高德开放平台 Web 服务 Key |
| `AMAP_WEATHER_URL` | 是 | 高德天气查询接口地址 |
| `MYSQL_DSN` | 是 | MySQL 连接串，格式 `mysql+pymysql://用户:密码@主机:端口/库名?charset=utf8mb4` |
| `JWT_SECRET` | 是 | JWT 签名密钥，**生产环境务必修改为随机字符串** |
| `JWT_EXP_DAYS` | 否 | JWT 过期天数，默认 7 |
| `NL2SQL_DB_PATH` | 否 | NL2SQL 示例 SQLite 库路径，默认 `./ops.db` |
| `FAULT_KB_PATH` | 否 | 故障诊断知识库 JSON 路径，默认 `./fault_kb.json` |

> ⚠️ 启动时 `database.py` 会校验 `MYSQL_DSN`、`JWT_SECRET`、`SILICONFLOW_API_KEY`、`SILICONFLOW_BASE_URL`、`SILICONFLOW_MODEL_NAME` 五个变量，缺失则直接报错退出。

### 2.2 安装后端依赖
```bash
pip install fastapi uvicorn sqlalchemy pymysql python-jose[cryptography] bcrypt python-dotenv langchain langchain-openai requests sqlparse
```

### 2.3 安装前端依赖
```bash
cd frontend
npm install
```

---

## 3. 数据库初始化

### 3.1 创建 MySQL 数据库并建表
```bash
# 登录 MySQL
mysql -u root -p

# 执行初始化脚本
source sql/init.sql
```
该脚本会创建 `agent_ops` 数据库及四张表：`users`、`sessions`、`messages`、`tool_call_logs`。

### 3.2 初始化 NL2SQL 示例库（SQLite）
```bash
python init_ops_db.py
```
执行后生成 `ops.db`，包含三张表示例数据：
- `servers`（5 台服务器）
- `metrics`（7 条指标）
- `alerts`（4 条告警）

> 注：`ops.db` 被 `.gitignore` 忽略，首次拉取代码后需运行此命令生成。

---

## 4. 启动服务

### 4.1 启动后端（FastAPI）
```bash
uvicorn controller:app --reload --host 127.0.0.1 --port 8000
```
- `--reload`：代码变更自动重载（开发环境）
- 后端监听 `http://127.0.0.1:8000`
- API 文档：`http://127.0.0.1:8000/docs`（Swagger UI）

### 4.2 启动前端（Vite Dev Server）
```bash
cd frontend
npm run dev
```
- 前端监听 `http://127.0.0.1:5173`
- `/api` 请求自动代理到后端 8000 端口

### 4.3 构建前端生产包（可选）
```bash
cd frontend
npm run build
```
产物输出到 `frontend/dist/`。

---

## 5. 使用指南

### 5.1 注册账号
1. 浏览器访问 `http://127.0.0.1:5173`
2. 首次访问会自动跳转到登录页，点击「注册」
3. 输入用户名和密码，提交后注册成功
4. 自动跳转到登录页

### 5.2 登录
1. 在登录页输入注册的用户名和密码
2. 登录成功后进入聊天主界面
3. Token 存储在 localStorage，刷新页面不丢失登录态

### 5.3 新建对话
- 点击左侧栏顶部的「+ 新对话」按钮
- 新建的会话默认标题为"新对话"，发送第一条消息后自动取消息前 12 字作为标题

### 5.4 切换对话
- 左侧栏展示当前用户的所有会话（按更新时间倒序）
- 点击任意会话切换，消息区加载该会话的历史消息

### 5.5 删除对话
- 将鼠标悬停在左侧栏的会话项上，右侧会出现垃圾桶图标
- 点击图标弹出确认框，确认后软删除该会话（`is_deleted=1`）
- 若删除的是当前正在查看的会话，消息区会自动清空

### 5.6 发送消息
- 在底部输入框输入内容
- `Enter` 发送，`Shift+Enter` 换行
- 发送中输入框禁用，AI 回复时显示"AI 正在思考..."

### 5.7 工具能力演示

#### 天气查询
> 输入：`北京今天天气怎么样？`

系统调用 `get_weather` 工具，通过高德 API 获取北京实时天气并回答。

#### 运维数据查询（NL2SQL）
> 输入：`CPU 使用率最高的服务器是哪台？`

系统调用 `nl2sql_query` 工具，LLM 生成 SQL 并经安全校验后查询 SQLite 示例库，返回结果。

#### 生成 SQL（不执行）
> 输入：`帮我写一个查询服务器 CPU 和内存的多表关联 SQL`

系统调用 `generate_sql` 工具，仅返回 SQL 语句，不执行查询。

#### 故障诊断
> 输入：`服务器 CPU 飙高了怎么办？`

系统调用 `diagnose_fault` 工具，匹配知识库返回分步骤排查清单和追问建议。

### 5.8 查看工具调用日志
- AI 回复气泡底部会展示「工具调用」折叠面板
- 展开可查看每次工具调用的名称、参数、结果、耗时

### 5.9 退出登录
- 点击右上角用户名 → 「退出登录」
- 清除 localStorage 中的 token，跳转到登录页

---

## 6. 常见问题排查

### 6.1 启动后端报错 `Missing required env vars`
- 检查根目录是否存在 `.env` 文件
- 确认 `MYSQL_DSN`、`JWT_SECRET`、`SILICONFLOW_API_KEY`、`SILICONFLOW_BASE_URL`、`SILICONFLOW_MODEL_NAME` 均已配置

### 6.2 启动后端报错 `ModuleNotFoundError: No module named 'xxx'`
- 安装缺失的依赖，例如：
  - `sqlparse` → `pip install sqlparse`
  - `jose` → `pip install python-jose[cryptography]`
  - `bcrypt` → `pip install bcrypt`

### 6.3 前端请求 401 Unauthorized
- 检查 Network 面板请求是否携带 `Authorization: Bearer <token>` 头
- 若 token 过期，重新登录即可
- 确认后端 `JWT_SECRET` 与签发时一致

### 6.4 前端请求 403 Forbidden
- 尝试访问了不属于当前用户的会话 ID
- 正常使用不会触发，若持续出现请检查会话归属

### 6.5 NL2SQL 提示 "SQL 安全校验未通过"
- LLM 生成的 SQL 包含黑名单关键词（DROP/DELETE/UPDATE 等）
- 或不是 SELECT 语句
- 尝试用更明确的自然语言描述查询需求

### 6.6 天气查询返回 "未配置AMAP_API_KEY"
- 检查 `.env` 中 `AMAP_API_KEY` 是否配置
- 高德 API Key 可在 https://console.amap.com/ 免费申请

### 6.7 AI 回复内容为空
- 后端有兜底逻辑，若 LLM 返回空会提示"抱歉，我暂时无法生成有效回复"
- 检查硅基流动 API Key 是否有效、额度是否充足

### 6.8 前端跨域问题
- 开发环境 Vite 已配置 `/api` 代理，直接访问 `http://127.0.0.1:5173` 即可
- 若前后端分离部署，需在后端 CORS 配置中添加前端域名

### 6.9 NL2SQL 查询失败 "no such table"
- `ops.db` 不存在或未初始化
- 运行 `python init_ops_db.py` 重新生成示例库

---

## 7. 开发与调试

### 7.1 后端热重载
```bash
uvicorn controller:app --reload
```

### 7.2 前端热更新
```bash
cd frontend && npm run dev
```

### 7.3 API 调试
访问 `http://127.0.0.1:8000/docs` 使用 Swagger UI 在线调试接口。

### 7.4 查看数据库记录
```sql
USE agent_ops;
SELECT * FROM users;
SELECT * FROM sessions ORDER BY updated_at DESC LIMIT 10;
SELECT * FROM messages WHERE session_id = 'xxx' ORDER BY id;
SELECT * FROM tool_call_logs WHERE session_id = 'xxx' ORDER BY id;
```

---

## 8. 部署说明

### 8.1 后端部署
```bash
# 生产环境建议关闭 --reload
uvicorn controller:app --host 0.0.0.0 --port 8000
```

### 8.2 前端部署
```bash
cd frontend
npm run build
# 将 dist/ 目录部署到 Nginx 或静态文件服务器
```

### 8.3 Nginx 反向代理示例
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```
